name: Code Coverage

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v3
